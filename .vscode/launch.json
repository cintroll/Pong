{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "codelldb",
      "request": "launch",
      "name": "Run Pong",
      "preLaunchTask": "Compile",
      "expressions": "native",
      "program": "${workspaceFolder}/build/bin/Pong"
    }
  ]
}
